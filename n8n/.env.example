# Example environment variables for n8n
# Copy this file to .env in the n8n directory and adjust values as needed

# Database password (if using PostgreSQL)
N8N_DB_PASSWORD=your_secure_password_here

# Basic auth credentials (if enabled)
N8N_BASIC_AUTH_PASSWORD=your_admin_password_here

# Encryption key for credentials (generate a secure random string)
N8N_ENCRYPTION_KEY=your_32_character_encryption_key_here